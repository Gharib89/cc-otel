"""Operator curation + ops tooling over the blob reservoir (#29).

Manual, on-demand scripts — not part of the sink runtime. Run as modules from the
repo root, e.g. ``uv run python -m tools.sweep``. See docs/agents/column-curation.md.
"""
