"""marimo + DuckDB notebook lab over the blob reservoir (#87, #249).

On-demand, local-only exploration upstream of the Power BI report: promotion-
candidate mining, redaction-leak auditing, ecosystem exploration, and reservoir/
marts overview. Notebooks are committed as ``.py`` (ruff-covered); outputs are
not. See ``analysis/README.md`` for how to run and the auth prerequisites.
"""
